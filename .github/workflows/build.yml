name: Build and Release

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: write
  packages: write

env:
  CARGO_TERM_COLOR: always
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
          - target: aarch64-unknown-linux-musl
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross
        run: |
          curl -L --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/cargo-bins/cargo-binstall/main/install-from-binstall-release.sh | bash
          cargo binstall cross -y

      - name: Build with cross
        run: cross build --release --target ${{ matrix.target }}
        env:
          RUSTFLAGS: "-C embed-bitcode=yes -C strip=symbols -C opt-level=z -C lto=true -C panic=abort -C codegen-units=1"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: brave-search-mcp-server-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/brave-search-mcp-server
          if-no-files-found: error

  docker:
    name: Build and Push Docker
    needs: build
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get short SHA
        id: slug
        run: echo "sha8=$(echo ${GITHUB_SHA} | cut -c1-8)" >> $GITHUB_OUTPUT

      - name: Download x86_64 artifact
        uses: actions/download-artifact@v4
        with:
          name: brave-search-mcp-server-x86_64-unknown-linux-musl
          path: target/x86_64-unknown-linux-musl/release

      - name: Download aarch64 artifact
        uses: actions/download-artifact@v4
        with:
          name: brave-search-mcp-server-aarch64-unknown-linux-musl
          path: target/aarch64-unknown-linux-musl/release

      - name: Fix permissions
        run: |
          chmod +x target/x86_64-unknown-linux-musl/release/brave-search-mcp-server
          chmod +x target/aarch64-unknown-linux-musl/release/brave-search-mcp-server

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ steps.slug.outputs.sha8 }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}

  release:
    name: Create Release
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get short SHA
        id: slug
        run: echo "sha8=$(echo ${GITHUB_SHA} | cut -c1-8)" >> $GITHUB_OUTPUT

      - name: Download x86_64 artifact
        uses: actions/download-artifact@v4
        with:
          name: brave-search-mcp-server-x86_64-unknown-linux-musl
          path: artifacts/x86_64

      - name: Download aarch64 artifact
        uses: actions/download-artifact@v4
        with:
          name: brave-search-mcp-server-aarch64-unknown-linux-musl
          path: artifacts/aarch64

      - name: Prepare release files
        run: |
          mkdir -p release_assets
          cp artifacts/x86_64/brave-search-mcp-server release_assets/brave-search-mcp-server-linux-amd64
          cp artifacts/aarch64/brave-search-mcp-server release_assets/brave-search-mcp-server-linux-arm64
          chmod +x release_assets/*

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: sha-${{ steps.slug.outputs.sha8 }}
          name: "Release ${{ steps.slug.outputs.sha8 }}"
          body: "Build for commit ${{ github.sha }} (${{ steps.slug.outputs.sha8 }})"
          prerelease: false
          files: release_assets/*
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
